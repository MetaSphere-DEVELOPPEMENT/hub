# /etc/modules-load.d/hub-uinput.conf — chargé au démarrage par systemd-modules-load.
# Le clavier et la souris virtuels de la télécommande téléphone (hub_pointeur.py) :
# sans ce module, /dev/uinput n'existe pas et la page du téléphone le dit.
uinput
